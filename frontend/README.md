# Frontend

This folder is for the Next.js single-page application for user login, device dashboards, and conversational interface.

To initialize a new Next.js app, run:

```
npx create-next-app@latest .
```
